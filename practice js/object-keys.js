const car={
    make:'Honda',
    model:'Accord',
    year:2020
}
const keys=Object.keys(car);
console.log(keys);
//  using the Object.keys() prototype method.when
//  you need to list the keys of an object.